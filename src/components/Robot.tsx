
import { ReactNode } from "react";

interface RobotProps {
  className?: string;
}

const Robot = ({ className = "" }: RobotProps) => {
  // Return null instead of rendering the robot image
  return null;
};

export default Robot;
